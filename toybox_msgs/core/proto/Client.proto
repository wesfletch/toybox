syntax = "proto2";

package core;

import "Message.proto";
import "Topic.proto";
service ToClient {


    rpc SendMessage(Message) returns (Null) {}
    rpc InformOfSubscription(TopicDefinition) returns (Confirmation) {}

    // rpc NegotiateConnection()
}

message Null {}